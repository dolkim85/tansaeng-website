# 탄생 스마트팜 라즈베리파이 Python 패키지

# 카메라 관련
picamera2>=0.3.12
libcamera>=0.0.5

# HTTP 요청
requests>=2.28.0

# 센서 라이브러리 (실제 센서 연결시 필요)
# Adafruit-DHT>=1.4.0
# adafruit-circuitpython-dht>=3.7.0
# adafruit-circuitpython-bh1750>=1.1.0
# adafruit-circuitpython-ads1x15>=2.2.21

# GPIO 제어
# RPi.GPIO>=0.7.1
# gpiozero>=1.6.2

# 이미지 처리
Pillow>=9.0.0

# 시리얼 통신 (센서와 통신시 필요할 수 있음)
# pyserial>=3.5

# JSON 처리 (기본 라이브러리이므로 설치 불필요)
# json

# 날짜/시간 처리 (기본 라이브러리이므로 설치 불필요)
# datetime

# 수치 계산
numpy>=1.21.0

# 로깅
logging

# 설정 파일 처리
configparser

# 암호화 (API 키 보안)
cryptography>=3.4.0